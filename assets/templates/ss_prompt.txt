Fix ONLY these issues in the SCENE TO REVIEW:
1. Block-boundary artifacts: orphaned words, duplicated lines,
   sentences cut mid-word at the start or end of the scene.
2. Malformed scene headings: should be INT./EXT. LOCATION — TIME
   in ALL CAPS on their own line. Fix capitalisation and punctuation
   only — do not rewrite the location or time.
3. Character name inconsistencies: use the knowledge base as ground
   truth. Fix spelling and capitalisation only.
4. Broken dialogue attribution: a speech with no character name above
   it, or the wrong character name — fix using context and knowledge base.
5. Truncated transitions: CUT TO:, FADE TO:, MATCH CUT TO:, SMASH CUT TO:
   etc. cut off at a block boundary — restore the full transition label
   with its colon.
6. Malformed INSERT or BACK TO SCENE labels: fix to ALL CAPS standard form.
7. Unknown or missing time specifier: if a scene heading contains UNKNOWN,
   or ends with only a location and no time (e.g. INT. OFFICE), infer the
   most likely time from the scene content and replace or add accordingly:
   office / meetings / daytime activity → DAY
   sleep / darkness / late-night atmosphere → NIGHT
   scene continuing directly without a time gap → CONTINUOUS
   default when genuinely ambiguous → DAY
   Never leave UNKNOWN in a scene heading.

Do NOT rewrite sentences, restructure action lines, change descriptions,
add content, or alter meaning in any way.
Do NOT add notes, comments, annotations, parenthetical remarks,
or any text not present in the original scene.
Do NOT explain what you changed.
If the scene has no issues, return it unchanged.
Output ONLY the corrected scene text, nothing else.
